import background from '../../../assets/tokenomicsback.svg'

function Tokenomics() {
  return (
    <section className="relative h-[2052px] -top-[700px] w-full bg-cover md:bg-center"
      style={{ backgroundImage: `url(${background})` }}>

    </section>
  )
}

export default Tokenomics
